import { ArrowRight, Check, GraduationCap } from "lucide-react";
import { useState } from "react";
import { Badge } from "@workspace/geslaagd-momentum/components/ui/badge";
import { Button } from "@workspace/geslaagd-momentum/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@workspace/geslaagd-momentum/components/ui/card";
import { Input } from "@workspace/geslaagd-momentum/components/ui/input";
import { Shell } from "./_shared/Frame";
export default function Onboarding() { const [level, setLevel] = useState("6 VWO"); const [done, setDone] = useState(false); const goals = ["Moeilijke onderwerpen begrijpen","Gerichter oefenen","Een achterstand inhalen"]; return <Shell dark className="grid place-items-center p-4"><Card className="w-full max-w-xl"><CardHeader><Badge variant="outline" className="w-fit"><GraduationCap /> korte start</Badge><CardTitle className="display text-3xl">Laat je leeromgeving met je meedenken.</CardTitle><p className="text-muted-foreground">Drie kleine keuzes, zodat je route straks beter bij je past.</p></CardHeader><CardContent className="space-y-7">{done ? <div className="py-8 text-center"><Check className="mx-auto text-primary" size={38} /><h2 className="display mt-4 text-2xl">Je profiel staat klaar.</h2><Button className="mt-6">Start mijn leeromgeving <ArrowRight /></Button></div> : <><fieldset><legend className="mb-3 text-sm font-medium">Waar sta je nu?</legend><div className="grid gap-2 sm:grid-cols-2">{["6 VWO","Eerstejaars bachelor"].map((item) => <Button key={item} variant={level === item ? "secondary" : "outline"} className="h-auto justify-start p-4 text-left" onClick={() => setLevel(item)}><span><strong className="block">{item}</strong><small className="text-muted-foreground">{item === "6 VWO" ? "Op weg naar je eindexamen." : "Onderwerpen uit je opleiding."}</small></span></Button>)}</div></fieldset><label className="block text-sm">Profiel of opleiding <Input className="mt-2" placeholder="Bijv. N&T met biologie" /></label><fieldset><legend className="mb-3 text-sm font-medium">Wat wil je vooral bereiken?</legend><div className="grid gap-2">{goals.map((goal) => <Button variant="outline" className="justify-start" key={goal}><Check className="text-primary" /> {goal}</Button>)}</div></fieldset><Button className="w-full" onClick={() => setDone(true)}>Start mijn leeromgeving <ArrowRight /></Button></>}</CardContent></Card></Shell>; }